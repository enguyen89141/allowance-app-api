CREATE TABLE children (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  first_name TEXT NOT NULL,
  last_name TEXT NOT NULL,
  email TEXT NOT NULL,
  parent_id INTEGER
    REFERENCES parents(id) ON DELETE CASCADE NOT NULL,
  login_id INTEGER
    REFERENCES logins(id) ON DELETE CASCADE NOT NULL
);